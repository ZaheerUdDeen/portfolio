---
const { title, subtitle } = Astro.props;
---

<div class="flex flex-wrap justify-between items-baseline gap-x-3">
  <h3 class="font-bold">{title}</h3>
  <span class="text-sm font-bold whitespace-nowrap">{subtitle}</span>
</div>
<p class="text-sm">
  <slot />
</p>
